{
  "name": "@pinecall/web",
  "version": "0.0.0",
  "description": "The browser's read of its own call: useCallState folds the public projection over livekit's DataChannel or the gateway's SSE door",
  "license": "Apache-2.0",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js"
    }
  },
  "files": ["dist"],
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "lint": "tsc -p tsconfig.test.json",
    "test": "vitest run"
  },
  "dependencies": {
    "@pinecall/protocol": "workspace:*",
    "use-sync-external-store": "^1.6.0"
  },
  "peerDependencies": {
    "livekit-client": "^2.22.0",
    "react": "^18.0.0 || ^19.0.0"
  },
  "devDependencies": {
    "@types/react": "^19.2.18",
    "@types/react-dom": "^19.2.7",
    "@types/use-sync-external-store": "^1.5.0",
    "jsdom": "^30.0.1",
    "livekit-client": "^2.22.3",
    "react": "^19.2.8",
    "react-dom": "^19.2.8",
    "typescript": "7.0.2",
    "vitest": "^5.0.0"
  }
}
