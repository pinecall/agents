{
  "name": "@pinecall/example-clinica-norte",
  "version": "0.0.0",
  "private": true,
  "description": "Cl\u00ednica Norte: one class, one view, one knowledge file \u2014 the tenant the design writes",
  "type": "module",
  "scripts": {
    "lint": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "pinecall": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "^26.4.1",
    "vitest": "^5.0.0"
  }
}
