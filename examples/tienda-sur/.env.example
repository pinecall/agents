# Where the gateway is, and as whom. Only for CI and containers: on a laptop `pinecall login`
# leaves the key in ~/.pinecall/credentials and a local dev gateway is found without either.
PINECALL_URL=wss://voice.pinecall.io
PINECALL_API_KEY=pk_live_...
